{
  "name": "colors-with-node",
  "version": "1.0.0",
  "description": "We're going to practice working with node modules and npm! We want to sort some song data to output the following:",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.1.2"
  }
}
